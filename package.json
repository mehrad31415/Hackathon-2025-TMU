{
  "name": "tfjs-basic-chrome-extension",
  "version": "0.3.0",
  "description": "Chrome Extension (MV3) using MobileNet + TFJS",
  "license": "Apache-2.0",
  "scripts": {
    "clean": "rm -rf dist node_modules && echo 'dist/ wiped'",
    "build": "node build.cjs",
    "watch": "nodemon --watch src --ext js,html,css --exec \"node build.cjs\""
  },
  "dependencies": {
    "@tensorflow-models/mobilenet": "^2.1.0",
    "@tensorflow/tfjs": "^3.13.0"
  },
  "devDependencies": {
    "esbuild": "^0.18.0",
    "nodemon": "^2.0.0"
  }
}